# TraductorRegex
TraductorRegex es un mini-lenguaje específico de dominio (DSL) que permite a los usuarios escribir patrones en lenguaje pseudonatural en inglés y obtener automáticamente la expresión regular (regex) equivalente. El objetivo principal es facilitar la creación de regex complejas sin necesidad de memorizar la sintaxis técnica.
